import { ExistingDataPipe } from './existing-data-pipe';

describe('ExistingDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ExistingDataPipe();
    expect(pipe).toBeTruthy();
  });
});
